import React from 'react';
import Layout from '../components/Layout';
import Landing from '../sections/Landing';

const app = () => {
  return (
    <Layout>
      <Landing />
    </Layout>
  );
};
export default app;
